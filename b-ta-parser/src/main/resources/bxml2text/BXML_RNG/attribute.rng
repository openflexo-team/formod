# DISTRIBUTED UNDER LICENCE GPL V3.0
# A RELAX NG compact syntax pattern
# for an attribute

Attribute =
	element Attr { anyElement* }
anyElement =
	element * {
  		(attribute * { text }
  		| text
  		| anyElement)*
	}

